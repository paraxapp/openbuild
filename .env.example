NODE_ENV=production
PORT=10000
# Render-only: single Postgres for users (auth). No DB projects.
DATABASE_URL=postgresql://user:pass@render-postgres-host/db?sslmode=require
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d
FRONTEND_URL=https://your-app.onrender.com
